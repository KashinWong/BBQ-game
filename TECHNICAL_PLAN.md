# 《烤串高手》灰盒原型技术方案（历史）

> 状态：已完成验证并归档。正式第 1 关实现见 `MVP_LEVEL_1.md` 与 `src/game/`。
> 原型问题：手机竖屏中，“点击食材自动上签—安排烤位—点击翻面—观察双面熟度—拖动出餐”的操作是否直观且有节奏？
> 布局结论：采用 A“上下工作台”；B、C 及开发态切换器已删除。

## 1. 技术选型

- Phaser 3：统一管理 Canvas 渲染、输入、动画和游戏时钟。
- TypeScript：让订单、烤串、食材和状态结构在快速迭代时保持可读。
- Vite：提供快速开发服务器和静态构建产物。
- 浏览器内存：原型不依赖账号、后端或本地存档，刷新即重置。

选择 Phaser 而不是 DOM 拖拽，是为了让触摸坐标、缩放、碰撞、动画和后续微信小游戏迁移尽量处于同一个渲染模型中。

## 2. 一条命令运行

```bash
npm install
npm run dev
```

浏览器访问终端给出的地址。手机与电脑处于同一局域网时，可通过开发服务器显示的 Network 地址在手机上试玩。

生产构建验证：

```bash
npm run build
npm run preview
```

## 3. 原型范围

### 本轮必须回答

- 移动食材的点击热区是否容易命中，自动上签反馈是否清楚。
- 点击翻面和拖动烤串是否会互相误触。
- 两个烤位与同时备料是否产生合理的多任务压力。
- 双面熟度是否能被看懂，何时出餐是否明确。
- 订单、烤架和备料区采用哪一种层级布局更适合手机。

### 本轮实现

- 牛肉、青椒、蘑菇三种灰盒食材。
- 一个动态订单、顾客耐心、90 秒倒计时。
- 一根手持签、一个待烤托盘、两个烤位和垃圾桶。
- 双面熟度、翻面、焦糊、配方校验、计分与连击。
- 获选的“上下工作台”竖屏布局。
- 重开本局和完整操作提示。

### 本轮不实现

- 正式美术、音效、粒子、关卡选择和存档。
- 六种完整食材、双订单、第三烤位和六关数值。
- 教学遮罩、账号、后端、排行榜、广告与微信 API。

## 4. 获选布局

采用 A“上下工作台”：订单在上、烤架居中、待烤托盘与垃圾桶位于烤架下方，动态备料区和当前签子位于底部。页面信息流与操作顺序一致：先看订单，再管理火候，最后点击食材备料。

原型评选结束后，B“订单侧栏”、C“烤架优先”、`?variant=` URL 分支和底部切换器均已删除，避免落选方案继续腐化或干扰后续开发。

## 5. 运行时状态

```text
GameScene
├─ RoundState：剩余时间、分数、连击、是否结束
├─ Order：配方、剩余耐心
├─ Ingredient[]：类型、位置、移动速度
├─ Skewer：食材列表、双面熟度、当前朝下侧、所在区域
├─ GrillSlot[2]：占用的烤串
└─ TraySlot：占用的生串
```

状态由单一 `GameScene` 持有，布局使用固定的 390×844“上下工作台”设计坐标。

## 6. 输入规则

- 点击备料区食材后，食材停止移动并在 180 毫秒内飞入当前签子的下一个空位。
- 食材使用大于视觉轮廓的点击热区；当前签子满载时拒绝继续拾取。
- 食材飞入期间短暂锁定当前签子，完成后恢复拖动。
- 在手持签或已有烤串上按下并移动超过阈值后进入拖动。
- 在烤串上短按且未超过拖动阈值时翻面。
- 松手时按烤位、托盘、垃圾桶、订单卡的优先级判定落点。
- 无效落点回到拖动前位置。
- 长按并停留垃圾桶清空整串，普通拖入只移除最后一块。

## 7. 时间与熟度

- 游戏使用 Phaser 的帧增量更新时间、耐心和当前朝下侧熟度。
- 订单耐心按配方动态计算：至少 32 秒，并包含食材数量、最慢食材双面烤制时间和固定舒适缓冲。
- 原型单面进入最佳区间的时间：青椒约 3.5 秒、蘑菇约 4 秒、牛肉约 5 秒。
- 原型用双侧进度条显式展示内部熟度，正式版本再替换为颜色、油光、烟和音效。
- 页面失焦或浏览器切到后台时暂停游戏时钟，回到页面后继续。

## 8. 微信小游戏迁移边界

- 核心玩法不直接读取 DOM，渲染和交互均由 Phaser 管理。
- 设计坐标固定为 390×844，通过 Phaser Scale 适配实际屏幕。
- 输入只依赖 Phaser Pointer，不依赖浏览器 Drag and Drop API。
- 后续把启动、音频、存档和生命周期放入平台适配层，即可分别接浏览器与微信 API。
- 正式项目应重新组织模块和补齐测试，不直接把原型代码当生产架构扩展。

## 9. 原型完成标准

- 手机尺寸浏览器中能够完整完成至少一个正确订单。
- 能串错、撤销、暂存、上架、翻面、烤焦、错误出餐和重开。
- 默认页面稳定呈现获选的“上下工作台”，不包含布局切换器或落选布局代码。
- `npm run build` 无 TypeScript 或打包错误。
- 试玩记录已保存布局结论；正式 MVP 开发时根据验证结果重写生产架构。
