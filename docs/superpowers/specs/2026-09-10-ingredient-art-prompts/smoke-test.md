# 食材提示词 smoke-test 与验收记录

日期：2026-09-10。

[返回方案](./README.md) · [完整提示词](./prompts.md) · [对接文档](./integration.md)

## 提示词文档初次检查

状态：通过。已在当前工作区执行一次性 Python 文档检查，并对新增文件执行 `git diff --no-index --check`。

| 检查 | 结果 |
| --- | --- |
| 食材与状态覆盖 | 与当前 IngredientKind 一致：6 种食材 × 3 态，18 条完整提示词，无重名或遗漏 |
| 提示词操作 | 6 条生成、12 条编辑；对应生图引用正确，熟态与焦态均独立从生图编辑 |
| 关键约束 | 18 条均包含透明 alpha、1024×1024、34×31 可读性及排除项，无待填写占位符 |
| 文档链接 | 5 份文档内的 42 处相对链接及锚点检查通过 |
| 当前代码依据 | 食材符号、备料与烤串尺寸、深色背景和场景配置已核对 |
| 格式 | 代码围栏配对、尾随空白及新增文档的 diff 检查通过 |
| 改动范围 | 写入前后原有 48 个文件的 SHA-256 均未改变，仅新增本 spec 目录中的 5 份文档 |

检查内容如下：

- 提示词必须覆盖当前 IngredientKind 的全部六种食材，各自 raw、cooked、burnt 三态，总计 18 条且无重名。
- 每条提示词完整展开，包含主体或状态描述、透明背景、小尺寸可读性及排除项，不残留待填写占位符。
- 六条生成提示词中只有生牛肉不带输入；其余五条只使用生牛肉图作画风参考。
- 十二条编辑提示词的输入必须是各自的生图，且保持轮廓、位置、尺寸、视角和透明背景。
- 文档内的相对链接、提示词锚点、代码入口均可解析。
- 比较写入前后的已有文件哈希，确保没有更改原有代码或其他未提交文件。
- 检查新增文档的尾随空白和 Markdown 代码围栏。

该段记录提示词文档初次交付时的检查；当时未运行图片生成或游戏运行时验收。图片质量不能由提示词数量检查证明，后续实际结果见本文件末尾。

## 生成任务文档检查

日期：2026-09-10。状态：通过。使用一次性 Node.js 文档检查核验本次新增 tasks.md 及目录入口。

- T001 至 T023 共 23 项任务，编号连续且唯一，均为待执行状态。
- 18 项食材图片任务与六种食材的 raw、cooked、burnt 提示词逐一对应，无重名或遗漏。
- 每项图片任务的输出名称正确；其余生图引用生牛肉，十二项编辑均引用各自生图。
- 当前 6 份文档中的 71 处相对链接及锚点已核验通过。
- tasks.md 与 README.md 的尾随空白、末尾换行及 diff 检查通过。

上述结果记录任务文档创建时的完整性与当时状态；后续已经执行 T001–T005，当前结果见下一节。

## 牛肉试样验收

日期：2026-09-10。范围：T001–T005。状态：通过牛肉试样验收，存在已记录的 T021 整理事项；其余 15 张食材图片及 T022 全量验收尚未执行。

### 文件与像素检查

使用 Pillow 实际解码三张 selected PNG，检查尺寸、RGBA、alpha 极值、透明与非透明像素、阈值包围盒、画布边缘和三态 alpha 一致性。熟态与焦态另与所选原始 RGB 候选逐像素比较，确认透明通道恢复没有改写生成图颜色。

| 状态 | 选定文件 | 所选 RGB 候选 | 尺寸与模式 | alpha | `alpha>=8` 包围盒 | 结果 |
| --- | --- | --- | --- | --- | --- | --- |
| 生 | [beef-raw.png](./generated/selected/beef-raw.png) | `beef-raw-v01.png` | 1254×1254 RGBA | `(0,255)`；826,896 个全透明像素 | `(107,161,1163,1105)` | 通过，作为画风与 alpha 基准 |
| 熟 | [beef-cooked.png](./generated/selected/beef-cooked.png) | `beef-cooked-v03.png` | 1254×1254 RGBA | 与生图逐像素相同 | `(107,161,1163,1105)` | 通过；RGB 与候选逐像素相同 |
| 焦 | [beef-burnt.png](./generated/selected/beef-burnt.png) | `beef-burnt-v04.png` | 1254×1254 RGBA | 与生图逐像素相同 | `(107,161,1163,1105)` | 通过；RGB 与候选逐像素相同 |

三态 alpha SHA-256 均为 `f8a0f2af79ed6d1a101e66898b379c0385b1f93fe40d48af6d9f39e958856ba6`，轮廓与锚点因此不存在像素漂移。生图 alpha 含 8 个落在画布底边的 `alpha=1` 噪点；`alpha>=8` 的主体不触边，目视不可见，留给 T021 统一清理。三图实际尺寸不是 1024×1024 目标，本节不把尺寸项记为通过，T021 必须等比归一化。

### 预览与目视检查

- [beef-states.png](./generated/qa/beef-states.png)：生、熟、焦三态并排，在白底与 `#241816` 深色底上检查边缘和状态。三态颜色差异清楚，无可见白边、投影、烟火、文字或额外物件。
- [beef-overlay.png](./generated/qa/beef-overlay.png)：生/熟与生/焦各 50% 叠放。外轮廓、位置和尺度一致，没有双边或跳动。
- [beef-small.png](./generated/qa/beef-small.png)：按 42×36 和 34×31 显示框，以 100% 大小展示白底、`#241816` 和 `#171412` 三种背景。三态均可辨认；焦态在深色背景上仍有棕橙轮廓和棕色区域。

| 检查项 | 当前状态 | 证据或限制 |
| --- | --- | --- |
| 文件完整性 | 牛肉通过；全量待执行 | 3/18 张选定 PNG 已存在，名称正确 |
| 尺寸 | 待 T021 | 三图同为 1254×1254，尚未归一化为 1024×1024 |
| 真透明 | 牛肉通过 | 三图均有 alpha 0 与非透明主体；熟、焦使用生图 alpha 恢复 |
| 边缘 | 牛肉通过 | 白底及两种深色底目视无白边；8 个底边 `alpha=1` 噪点待清理 |
| 画风一致 | 牛肉试样通过 | 深棕轮廓、斜俯视和状态色一致；焦态内部纹理略密，后续全量时复核 |
| 三态对齐 | 牛肉通过 | alpha 通道逐像素相同，50% 叠放无位置跳动 |
| 备料尺寸 | 牛肉通过 | 42×36 实际大小下生、熟、焦均可辨认 |
| 烤串尺寸 | 牛肉通过 | 34×31 实际大小下生、熟、焦均可辨认 |
| 易混食材 | 待执行 | 鸡肉、香肠和玉米尚未生成 |
| 焦态 | 牛肉通过 | 深色底仍可辨认，未变成纯黑块 |
| 内容限制 | 牛肉通过 | 每张只有一块牛肉，无竹签、场景、文字、五官或拼图 |

内置编辑生成的熟态和焦态原始候选均为无 alpha 的 RGB PNG，并带有可视化透明网格。连续定向返修仍未得到原生 alpha，因此选定图保留最终候选 RGB 像素，并使用同一张生图 alpha 恢复透明通道。完整调用、提示词、候选版本与后处理记录见[生成记录](./generated/generation-log.md)。本次未运行游戏接入或运行时验收。
