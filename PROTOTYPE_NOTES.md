# 灰盒原型试玩记录

> PROTOTYPE VERDICT：采用 A“上下工作台”。用户于 2026-08-06 明确选择；B、C 和布局切换器已从代码中删除。

## 运行

```bash
npm install
npm run dev
```

直接访问 `/`，默认进入获选的“上下工作台”。

## 最终结论

- 获胜变体：A“上下工作台”
- 结构：订单在上、烤架居中、工具区居中偏下、备料区在底部
- 选择依据：用户试玩后明确采用 A；未提出混合其他变体元素
- 清理结果：B、C、`?variant=` 分支、底部切换器和键盘切换逻辑均已删除
- 后续仍需调试：翻面/拖动阈值与烤制时间

## 自动化检查记录（2026-08-06）

- 390×844：A、B、C 三种布局均正常渲染，无控制台错误。
- 320×568：布局完整缩放到视口内，关键区域没有越界；文字偏小，属于极小屏试玩时需要关注的问题。
- 已实际触发：串取两种食材、放入烤位、点击翻面、错误配方拒收、正确配方出餐、订单刷新与本局重开。
- 已验证底部按钮从 C 循环切换到 A，URL 正确更新为 `?variant=A`。
- `npm run typecheck` 通过。
- `npm run build` 通过；当前 Phaser 单包约 1.50 MB、gzip 后约 347 KB，原型阶段接受，正式版本需要做加载与资源预算。

自动化操作跨调用时会持续消耗真实游戏时间，因此正确配方出餐验证得到的是焦糊低分，而不是完美分。这证明配方和出餐链路可用，但“最佳熟度窗口是否舒服”仍必须由真人连续操作试玩判断。

## 试玩反馈修正（一，已被点击拾取方案取代）

反馈：食材距离初始签子太近，拿起即可能误串；同时签尖难以瞄准，容易碰到错误食材。

根因与修正：

- 布局 C 的底部食材轨道原为 `y=749`，签子为 `y=750`。现统一为三个布局预留至少 72px 的无食材安全走廊。
- 原规则拖动超过 8px 就允许串取。现增加 24px 出鞘距离，小幅拿起不会触发食材。
- 原命中使用半径 31px 的圆形吸附。现改为签尖运动线段穿过 34×28px 食材核心区域，减少斜角误碰，同时保留快速划过时的命中。
- 签尖增加黄色瞄准环；当前订单所需食材增加黄色描边，降低视觉选择成本。
- 新增四项交互几何回归检查，覆盖三个布局的安全走廊、出鞘距离、快速穿刺和斜角近失。

以上签尖轨迹方案在继续试玩后仍受固定签子长度限制，现已整体删除，不再作为当前交互规则。

## 试玩反馈修正（二，2026-08-06）

反馈：固定长度签子导致部分食材不可达，串取动作别扭；订单耐心与烤制时间接近，容易刚烤好就超时。

当前方案：

- 备料改为直接点击食材，食材在 180 毫秒内自动飞入签子的下一个空位。
- 食材点击热区扩大到 54×48px；签子满载或食材正在飞入时提供明确提示。
- 首轮实现曾因自定义点击矩形与 Phaser 容器原点重复偏移，造成食材只有边缘可点；已改为由容器尺寸自动生成标准热区。
- 删除签尖碰撞、出鞘距离、扫过食材等旧规则及相关回归检查。
- 订单耐心从固定 25 秒改为动态公式：`max(32, ceil(16 + 食材数 × 3 + 最慢单面烤制秒数 × 2.5))`。
- 当前两块订单约 32～35 秒，三块慢食材订单约 38 秒；耐心条上方直接显示剩余秒数。
- 新增订单耐心回归检查，确保食材更多或烤制更慢的订单获得更多时间。

浏览器复测：连续点击牛肉和青椒后，签子按顺序显示两块食材并可正常拖入烤位；订单初始耐心为 35 秒，自动化拾取和拖动消耗约 13 秒后仍剩余 22 秒。390×844 下无控制台错误。
